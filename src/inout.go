package example

func Print(msg string) {
	// os.Stdout.WriteString(msg + "\n")
	// fmt.Println("Printed message:", msg)
	// writer := bufio.NewWriter(os.Stdout)
	// defer writer.Flush()
	// writer.WriteString(msg + "\n")
	// fmt.Printf("message with double quotes: %q\n", msg)
	// fmt.Println("Give me two numbers:")
	// var a, b int
	// fmt.Scanln(&a, &b)
	// fmt.Printf("%d + %d = %d\n", a, b, a+b)
	// reader := bufio.NewReader(os.Stdin)
	// fmt.Fscanf(reader, "%d %d\n", &a, &b)
	// fmt.Printf("%d + %d = %d\n", a, b, a+b)
	// for i, c := range "nihao" {
	// 	fmt.Printf("index: %d, char %c\n", i, c)
	// }
	// arr := make([]int, 5)
	// arr[0] = 10
	// arr[1] = 20
	// arr[2] = 30
	// arr[3] = 40
	// arr[4] = 50
	// for i, v := range arr {
	// 	fmt.Printf("%d: %d\n", i, v)
	// }
	// var num []int
	// num = append(num, 1)
	// fmt.Println(num[0])
	// num1 := []int{2,3,4}
	// num1 = make([]int, 3)
	// num = new([]int)
	// num1 := slices.Clone(num[:])
	// num1[0] = 100
	// fmt.Println(num[0])
	// fmt.Println(num1[0])
}
